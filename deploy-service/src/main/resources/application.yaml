server:
  port: 8084

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/devflow_deploy_db
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true