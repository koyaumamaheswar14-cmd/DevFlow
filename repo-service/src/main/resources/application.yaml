server:
  port: 8081

spring:
  application:
    name: repo-service

  datasource:
    url: jdbc:mysql://localhost:3306/devflow_repo_db
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true